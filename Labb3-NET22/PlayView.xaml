<UserControl x:Class="Labb3_NET22.PlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_NET22"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="3" Content="{Binding Quiz.Title}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" ContentStringFormat="Playing Quiz: {0}"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <Label x:Name="LabelQuestionCategory" Grid.Row="0" FontSize="14" FontWeight="Bold" Content="{Binding CurrentQuestion.Category}" ContentStringFormat="Category: {0}"/>
            <Label x:Name="LabelPercentageCorrect" Grid.Row="1" FontSize="14" FontWeight="Bold" Content="{Binding PercentageCorrect}" ContentStringFormat="Percentage correct: {0}%"/>
            <Label x:Name="LabelNumberCorrect" Grid.Row="2" FontSize="14" FontWeight="Bold" Content="{Binding NumberCorrectAnswers}" ContentStringFormat="Number of correct answers: {0}"/>
            <Label x:Name="LabelQuestionsAnswered" Grid.Row="3" FontSize="14" FontWeight="Bold" Content="{Binding QuestionsAnswered}" ContentStringFormat="Total questions answered: {0}"/>

        </Grid>

        <Label x:Name="LabelQuestionStatement" Grid.Row="1" Grid.ColumnSpan="3" Content="{Binding CurrentQuestion.Statement}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" ContentStringFormat="Question: {0}"/>
        <Label x:Name="LabelQuizComplete" Grid.Row="1" Grid.ColumnSpan="3" Content="QUIZ COMPLETED" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Visibility="Hidden"/>

        <Image x:Name="ImageQuestion" Grid.Row="2" Grid.Column="1" Source="{Binding CurrentImage}" Stretch="Fill"/>
        <ScrollViewer Grid.Row="3" Grid.Column="1">
            <StackPanel x:Name="StackPanelAnswerButtons" Background="White"/>
        </ScrollViewer>

        <Button x:Name="ButtonReturnToMainMenu" Grid.Row="4" Grid.Column="1" Content="Return To Main Menu" Click="ButtonReturnToMainMenuClicked" IsEnabled="False"/>
    </Grid>
</UserControl>
