<UserControl x:Class="Labb3_NET22.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_NET22"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Quiz Game" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Play" Click="ButtonPlayClicked" Height="30"/>
            <Button Content="Questions By Category" Click="ButtonQuestionsByCategoryClicked" Height="30"/>
            <Button Content="Create New Quiz" Click="ButtonCreateNewQuizClicked" Height="30"/>
            <Button Content="Edit Quiz" Click="ButtonEditQuizClicked" Height="30"/>
            <Button Content="Remove Quiz" Click="ButtonRemoveQuizClicked" Height="30"/>
            <Button Content="Save Quizzes" Click="ButtonSaveQuizzesClicked" Height="30"/>
            <Button Content="LoadQuizzes" Click="ButtonLoadQuizzesClicked" Height="30"/>
            <Button Content="Exit" Click="ButtonExitClicked" Height="30"/>
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Content="Available Quizzes" HorizontalAlignment="Center" FontWeight="Bold"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="lstBoxAvailableQuizzes" SelectionChanged="QuizChanged"/>
                </ScrollViewer>

                <Label Grid.Column="1" Content="Questions in quiz" HorizontalAlignment="Center" FontWeight="Bold"/>
                <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <ListBox x:Name="lstBoxQuizQuestions"/>
                </ScrollViewer>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="Available Categories" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                <Label Grid.Row="1" Content="{Binding QuestionsInCheckedCategories}" HorizontalAlignment="Center" FontSize="10" FontWeight="Bold" ContentStringFormat="Questions in the selected categories: {0}"/>
                <TextBlock Grid.Row="2" 
                           Text=
                           "Select one or more categories to have random questions of the selected categories shown when playing 'Questions By Category'. The questions and categories will be taken from the questions available in the created quizzes."
                           TextWrapping="Wrap"/>
            </Grid>
            
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="wrapPanelCategories" HorizontalAlignment="Center" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
